query($ident: ID!) {person: hentPerson(ident: $ident) {
    adressebeskyttelse {
        gradering
    }
    geografiskTilknytning {
        gtType
        gtKommune
        gtLand
        gtBydel
    }
}}
